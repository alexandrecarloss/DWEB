<div class="col-md-6 col-lg-6 col-xl-4">
    <div class="rounded position-relative fruite-item">
        <div class="fruite-img">
            <img src="{{ pet.image_url }}" class="img-fluid w-100 rounded-top" alt="">
        </div>
        <div class="text-white bg-secondary px-3 py-1 rounded position-absolute" style="top: 10px; left: 10px;">{{ pet.category }}</div>
        <div class="p-4 border border-secondary border-top-0 rounded-bottom d-flex justify-content-between align-items-center">
            <div>
                <p class="text-secondary" style="font-size: 10px;margin-bottom: 3px;">{{ pet.owner }}</p>
                <h4>{{ pet.name }}</h4>
                <p>{{ pet.location }}</p>
                <div class="d-flex justify-content-between flex-lg-wrap">
                    <a href="#" class="btn btn-primary btn-block rounded-pill px-3"> Quero adotar</a>
                </div>
            </div>
            <div class="sexo">
                <img src="{{ pet.gender_image_url }}" alt="{{ pet.gender }}" loading="lazy" style="width: 20px;">
            </div>
        </div>
    </div>
</div>
